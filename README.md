# ssl-pso-cifar10
Semi-supervised image classifier trained on only 10% labeled CIFAR-10 data using iterative pseudo-labeling, with Particle Swarm Optimization to auto-tune hyperparameters. Built with PyTorch.
